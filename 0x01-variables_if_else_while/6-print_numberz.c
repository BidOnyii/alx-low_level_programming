#include <stdio.h>

/**
 * main - program that prints all single digit numbers of base 10 starting from 0,
 * You are not allowed to use any variable of type char
 * You can only use the putchar f
	putchar('\n');
	return (0);
}
